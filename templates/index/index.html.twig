{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    {% if app.user %}
        <div class="mb-3">
            {% trans %}You are logged in as{% endtrans %} {{ app.user.pseudo }}. <a href="{{ path('app_logout') }}">{% trans %}Logout{% endtrans %}</a>
        </div>
        <div>
            <h1>Création de partie</h1>
            <a href="{{ path('player_invite') }}">{% trans %}Commencer{% endtrans %}</a>
        </div>
    {% else %}
        <div class="mb-3">
            <h1>The Rumor - Le jeu du téléphone</h1>
        </div>
    {% endif %}
</div>
{% endblock %}
